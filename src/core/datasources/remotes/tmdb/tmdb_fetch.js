//import { TMDB_API } from "./tmdb_api";



/*
export const fetchApiTMDB = async (url, query) => {
    try {
        const response = await TMDB_API.get(url, {
            params: {
                query: query,
            }
        });
        const data = await response.data;
        return data;
    } catch {
        return "error";
    } finally {
        console.log('Proceso Terminado');
    }
}

export const fetchApiImgTMDB = async (url, query) => {
    try {
        const response = await TMDB_IMG_API.get(url);
        const data = await response.data;
        return data;
    } catch {
        return "peticion img error";
    } finally {
        console.log('IMG: Proceso Terminado');
    }
}
*/

